{
  "name": "nft-minting-engine",
  "version": "1.0.0",
  "description": "Configurable NFT minting engine with IPFS metadata storage. Solidity + Hardhat + Pinata.",
  "author": "Kai Silva",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "@pinata/sdk": "^2.1.0"
  },
  "devDependencies": {
    "hardhat": "^2.19.0",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0"
  }
}